# PoC 验收样本与记录

已提交的样本位于仓库根目录 `example/`。含敏感内容的真实教案、原始任务数据和临时调试产物不得提交。

## 2026-08-31 清除正文复核提示

- 删除段落诊断字段及生成、展示和导出逻辑。未知纯文本和不规则多栏保留原文；明确删除且几何唯一匹配的跨页表格空片段不生成占位，仍拒绝缺失资源、无法转换结构及无可用内容的结果。
- 使用项目 Java 17.0.20.1、Maven Wrapper 3.9.11 执行 `./mvnw test`：127 项通过，无失败、错误或跳过。契约测试确认段落仅包含 id、type、text，数据库读写仍正常；跨页片段测试覆盖不同坐标尺度和后续正文顺序。
- Node 24.14.0、pnpm 10.28.0 下 lint、25 项测试和构建通过，仍有主包超过 500 kB 的体积警告。Web 阅读、编辑和导出 HTML 的正文内容与顺序通过 DOM 测试验证，未进行浏览器或 DOCX 视觉验收。
- 未调用 MinerU、未修改本地数据库或原始文件；已有开发文档需重新解析才能清除存储中的旧提示字段和空段落，不添加迁移或兼容逻辑。已移除不再适用的提示数量和输出块数回放记录。

## 2026-08-31 DOCX 导出泛化修复

- 使用项目 Java 17.0.20.1、Maven Wrapper 3.9.11 执行 `./mvnw test`：127 项通过，失败、错误、跳过均为 0。其中新增 10 项通用回归，覆盖单元格多段落与列表、嵌套表格无重复且宽度受父单元格约束、独立/嵌套列表起号和续段顺序、六级标题及空白、损坏图片、超窄表格及不支持的列表格式、公式装饰及未知 MathML 拒绝。图片失败验证了单篇及批量 HTTP 422，无下载响应头或部分 ZIP。
- 使用 Node 24.14.0、pnpm 10.28.0 执行 `pnpm lint`、`pnpm test`（24 项）、`pnpm build`：全部通过。仍存在主包超过 500 kB 的构建警告。
- 通过实际 Java 导出器生成两份与真实教案无关的文档：结构组合文档 2 页；含合并单元格图片、24 条中英文记录的长表文档 5 页。使用 documents 技能的 `render_docx.py` 渲染，逐页检查全部 7 页：公式上/下划线及重音可见，中文与换行正常，独立列表从 1 开始、续段缩进正确，内表无重复且不越出父单元格，图片等比缩放，长表重复表头和跨页续文正常。临时 DOCX、PDF、PNG 和生成脚本不进入仓库。
- 已知视觉边界：LibreOffice 对嵌套表格前标题的 `keepNext` 请求未完全遵从，父表行跨页时标题仍可能留在前页；本轮没有进行 Microsoft Word 客户端验证。不把 XML 结构通过或这两份构造文档解释为任意 PDF 的视觉保真保证。
- 不修改协议字段，不重新调用 MinerU，不清空本地项目数据库；契约测试使用独立 Testcontainers MySQL。已有文档重启后端后重新导出即可使用修复，无需重新解析。

## 2026-08-31 泛化修复验证

### 自动化验证

- Java 17（项目 `.sdkmanrc`）与 Maven Wrapper 3.9.11 下执行 `./mvnw test`：117 项通过，失败、错误、跳过均为 0。契约测试使用独立 Testcontainers MySQL，不清空或修改本地项目数据库。
- 使用项目声明的 Node 24.14.0、pnpm 10.28.0：`pnpm lint`、`pnpm test`（24 项）、`pnpm build` 全部通过。
- 前端构建仍有主包超过 500 kB 的体积警告，本轮不涉及拆包。

新增或调整的通用验证不依赖样例文件名、章节、正文或固定页码：

- 英文标题、独立正文和数据表保持上游块边界；即使左侧对齐、紧邻表格或内部段落间距很大，也不猜测性合并。
- 相同几何在 0～1 与 0～1000 两种坐标单位下产生相同边界、标题对齐和多栏结果；多栏另覆盖不同页面尺寸。
- 空结果、非对象块、缺图（含表格内图片）、无结构表格、无效列表条目及无法完整转换的嵌套内容明确失败，不保存为成功文档。
- 未知纯文本转换为普通 paragraph；序列化往返只保留 id、type、text，不泄漏 Provider 字段或附加诊断信息。
- 不规则长短栏、短栏及混合公式 span 保留原文；普通词间距和缺少版面信息不会触发强制重排。
- 用独立构造的跨页表格验证 `lines_deleted` 标记、页面尺寸和几何唯一匹配；明确删除的空片段跳过且不产生占位，前后正文顺序不变；无标记、错误页码/位置、重复匹配或仍有行内容时，不猜测为空续片。整份文档只有已删除空片段时仍失败。
- 表格内无引号的图片路径也能归档重写；缺图不保留失效路径冒充成功。
- Web 阅读、编辑和导出 HTML 均只输出正文，段落内容和顺序一致。

### 验证边界与后续验收

- 已保存文档不会自动重算；运行更新后的后端并重新上传，才能获得新的边界和完整性检查。不为旧开发数据引入迁移或双读逻辑。
- 删除了按几何猜测的表格前置文字合并及其专用样式。原先被人为包进表格的标题、正文现在独立输出，不再承诺这些区域还原成原 PDF 表格边框。
- 跨页片段、未知文本和可检测的不规则多栏仍需人工复核；没有检测到的复杂版面仍依赖 Provider 顺序。
- 独立扫描件、旋转、裁切、低清晰度、多语言及跨块复杂多栏尚未专项验收。当前只支持 PDF，不支持直接上传手机照片。
- 尚无独立留出集和逐字/结构标注，不能把以上结果解释为任意 PDF 的泛化准确率。后续应按文档模板、页面尺寸、扫描质量和布局类型分层选取未用于开发的样本，分别记录文字错误率、内容遗漏、阅读顺序及表格结构错误；真实 Provider 端到端测试需要有效 API 凭据，密钥不得写入结果或仓库。

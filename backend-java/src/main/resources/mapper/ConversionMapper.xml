<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lessonweb.lesson.persistence.mapper.ConversionMapper">
  <select id="selectActiveById" resultType="com.lessonweb.lesson.persistence.entity.ConversionEntity">
    SELECT * FROM lesson_document_conversion WHERE id = #{id} AND del_flag = '0'
  </select>
  <update id="updateState">
    UPDATE lesson_document_conversion
    SET status = #{status}, error_code = #{errorCode}, msg = #{msg},
        completed_at = #{completedAt}, updated_at = #{updatedAt}
    WHERE id = #{id} AND del_flag = '0'
  </update>
  <update id="markInterrupted">
    UPDATE lesson_document_conversion
    SET status = 3, error_code = 'SERVICE_RESTARTED', msg = '服务重启，解析任务未完成',
        completed_at = #{updatedAt}, updated_at = #{updatedAt}
    WHERE del_flag = '0' AND status IN (0, 1)
  </update>
  <update id="softDelete">
    UPDATE lesson_document_conversion SET del_flag = '2', updated_at = #{updatedAt}
    WHERE id = #{id} AND del_flag = '0' AND status = 2
  </update>
</mapper>

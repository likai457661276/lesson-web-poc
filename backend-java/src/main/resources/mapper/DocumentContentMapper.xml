<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lessonweb.lesson.persistence.mapper.DocumentContentMapper">
  <select id="selectActiveById" resultType="com.lessonweb.lesson.persistence.entity.DocumentContentEntity">
    SELECT c.* FROM lesson_document_content c
    INNER JOIN lesson_document_conversion j ON j.id = c.conversion_id
    WHERE c.id = #{id} AND j.del_flag = '0' AND j.status = 2
  </select>
  <select id="selectActiveSummaries" resultType="com.lessonweb.lesson.persistence.entity.DocumentSummaryRow">
    SELECT c.id, c.title, c.source_file_name AS sourceFileName, c.source_type AS sourceType,
           c.block_count AS blockCount, c.created_at AS createdAt, c.updated_at AS updatedAt
    FROM lesson_document_content c
    INNER JOIN lesson_document_conversion j ON j.id = c.conversion_id
    WHERE j.del_flag = '0' AND j.status = 2
    ORDER BY c.updated_at DESC
  </select>
  <update id="updateActive">
    UPDATE lesson_document_content c
    INNER JOIN lesson_document_conversion j ON j.id = c.conversion_id
    SET c.version = #{entity.version}, c.title = #{entity.title}, c.source_type = #{entity.sourceType},
        c.source_file_name = #{entity.sourceFileName}, c.block_count = #{entity.blockCount},
        c.content_json = #{entity.contentJson}, c.updated_at = #{entity.updatedAt}
    WHERE c.id = #{entity.id} AND j.del_flag = '0' AND j.status = 2
      AND CAST(c.content_json AS BINARY) = CAST(#{expectedJson} AS BINARY)
  </update>
</mapper>

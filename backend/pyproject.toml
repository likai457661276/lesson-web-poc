[project]
name = "backend"
version = "0.1.0"
description = "MinerU to LessonDocument v1 parsing API."
readme = "README.md"
requires-python = ">=3.12,<3.13"
dependencies = [
  "fastapi>=0.115.12,<1.0.0",
  "httpx>=0.28.1,<0.29.0",
  "pydantic-settings>=2.9.1,<3.0.0",
  "python-multipart>=0.0.20,<1.0.0",
  "lark>=1.2.2,<2.0.0",
  "sympy>=1.14.0,<2.0.0",
  "uvicorn[standard]>=0.34.2,<0.35.0",
  "beautifulsoup4>=4.13.4,<5.0.0",
  "python-docx>=1.1.2,<2.0.0",
  "latex2mathml>=3.81.0,<4.0.0",
  "mathml2omml>=0.0.2,<0.1.0",
]

[dependency-groups]
dev = [
  "pytest>=8.3.5,<9.0.0",
  "pytest-asyncio>=0.26.0,<1.0.0",
]

[build-system]
requires = ["hatchling>=1.27.0"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["app"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
asyncio_mode = "auto"

[tool.uv]
default-groups = ["dev"]

[[tool.uv.index]]
name = "aliyun"
url = "https://mirrors.aliyun.com/pypi/simple/"
default = true
